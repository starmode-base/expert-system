---
name: format
description: Format code using prettier with bun run format
---

Format the codebase using prettier:

```bash
bun run format
```

This command runs prettier across the entire project to ensure consistent code formatting.
