# TanStack Start
**/build
**/public
pnpm-lock.yaml
routeTree.gen.ts

# Drizzle migrations
drizzle/meta

# Build output and Vercel artifacts
.output
.vinxi
.vercel

# Vercel rewrites this file (minified, with name/version added) inside its
# build container, so `prettier --check .` would always fail there.
vercel.json
